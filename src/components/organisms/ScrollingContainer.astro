---
import CardProcess from "../../components/molecules/CardProcess.astro";
import process from "../../data/process.json";
---

<ul class="grid gap-8 md:gap-20">
  {
    process.map((step) => {
      return <CardProcess data={step} />;
    })
  }
</ul>
